TestMind V2-P09.1 Implementation Prompt
开始 TestMind V2-P09.1。
本轮实施已经完成审计后的新产品方向：
P09.1 — Functional Case Domain Convergence
目标：
将现有 V1「功能用例管理」正式迁移到 V2 TestArtifact，使项目的模块树和功能用例只有一个 Source of Truth。

P09.1 完成以后：
Project
↓
Primary Functional TestArtifact
↓
root
├─ module
│  ├─ module
│  │  └─ test_case
│  └─ test_case
└─ module
现有「功能用例管理」页面必须能够：
选择项目
↓
看到嵌套 Module Tree
↓
点击 Module
↓
看到该 Module/subtree 下的 Case List
本阶段不做 MindMap、不做 Diff UI、不做 AI 协作。
1. Source of Truth
从本阶段开始：
TestArtifact
ArtifactNode
ArtifactRevision
ArtifactOperation
是功能测试模块与功能用例唯一 Source of Truth。
禁止：
test_modules
        ↕
ArtifactNode

function_cases
        ↕
ArtifactNode
长期双写。
明确：
NO DUAL-WRITE
2. 产品 Domain 正式收敛
审计阶段曾建议底层继续：
root
group
test_point
test_case
本轮不采用该长期方案。
因为当前：
test_modules = 0 rows
function_cases = 0 rows
而最终产品已经明确只有：
Module
TestCase
因此现在直接把 ArtifactNode node_type 收敛为：
root
module
test_case
3. 为什么现在改 node_type
不要因为：
已有测试多
P08 Tool 使用旧 enum
就留下长期：
group
test_point
产品概念债务。
现在尚未产生真实 Artifact 大量数据，是最低成本收敛窗口。
修改必须完整覆盖：
Artifact schemas
Artifact service validation
Artifact replay
Undo/Restore
Artifact Tools
Quality tools
Test Design Skill
P08 Eval fixtures
MindMap adapter
tests
但：
AgentLoop
ConversationRunner
Worker
Revision architecture
不需要改变。
4. Migration
新增正式 Alembic migration，编号按当前 HEAD 自动确认。
目标：
已有 ArtifactNode:
group      → module
test_point → module
如果测试/dev 数据中已经存在这些节点：
必须 migration 转换。
不要假设只有 V1 表是空的就忽略已有 TestArtifact 数据。
迁移后 node_type 合法值：
root
module
test_case
5. Tree integrity 更新
最终规则：
root
唯一
不可删除
不可移动
parent=NULL
module
可以：
parent = root
或
parent = module
支持任意合理深度嵌套。
test_case
可以：
parent = module
不能：
parent=root
不能成为父节点。
因此：
Case 必须属于 Module
成为 Domain invariant。
6. 删除“无模块 Case”语义
不再支持：
test_case.parent_id = root
也不再提供：
无模块用例
产品入口。
如果创建 Case 时调用方没有 Module：
Application Service / 产品 service 必须先：
ensure default module
然后把 Case 放进去。
7. Default Module
默认一级 Module：
默认模块
仅在：
调用方创建 Case
但没有指定 module
且无法从上下文推断模块
时创建。
必须保证同一 Artifact 不会并发创建多个：
默认模块
默认模块
默认模块
实现一个明确 service，例如：
get_or_create_default_module()
使用事务/数据库约束或安全条件裁决。
8. Project 与 Functional Artifact
产品正式采用：
一个 Project 默认一份 Primary Functional TestArtifact。

结构：
Project
└─ Functional TestArtifact
   └─ root
      └─ modules/cases
不要每个 Module 一个 Artifact。
9. ensure_project_functional_artifact()
建立明确 Application Service：
get_project_functional_artifact()
ensure_project_functional_artifact()
首次打开项目功能用例页：
没有 Artifact
→ 创建 Primary Functional Artifact
→ 创建 root
之后重复调用：
返回同一 Artifact
必须考虑并发创建。
两个浏览器同时打开空项目：
最终只能产生一份 Primary Functional Artifact
优先用数据库唯一约束保证，而不是 Python global lock。
10. Artifact Type
审计当前实际 artifact_type。
为 Functional Case Domain 选择一个稳定值。
例如：
functional_test
如果现有 P07/P08 已普遍使用：
test_design
可以继续使用，但必须明确其产品意义：
Project primary functional test artifact
如果改变 artifact_type 会导致大量无意义迁移，可暂不改名。
重点是：
(project_id, functional artifact purpose)
必须可唯一识别。
11. Case Number
不要沿用旧：
case_code
由用户或 LLM 任意填写。
也不要按：
order_key
动态生成 TC001。
第一版统一稳定展示号：
TC-{ArtifactNode.id:06d}
例如：
TC-000123
特点：
- Server identity 派生；
- 永久稳定；
- 移动 Module 不变；
- reorder 不变；
- 不需要抢号；
- LLM 无法制造重复；
- 不额外增加 DB 列。
建立统一 helper：
format_case_number(node_id)
Backend/frontend 不要各写一套不同格式。
12. TestCase Schema 正式收敛
目标 TestCase：
number        derived from node.id
module        parent module
title
preconditions
steps
expected_results
priority
tags
source_refs
推荐 content：
{
  "preconditions": [
    "用户账号已注册"
  ],
  "steps": [
    {
      "step_no": 1,
      "action": "进入登录页面",
      "data": null
    },
    {
      "step_no": 2,
      "action": "输入正确账号和密码",
      "data": null
    },
    {
      "step_no": 3,
      "action": "点击登录",
      "data": null
    }
  ],
  "expected_results": [
    {
      "step_no": 3,
      "expected": "登录成功并进入首页"
    }
  ],
  "priority": "P1",
  "tags": []
}
13. Priority
统一支持：
P0
P1
P2
P3
默认：
P1
Service/schema 必须验证。
不要仅靠 UI dropdown。
14. Steps Schema
不再允许：
Optional[Any]
进入新的 TestArtifact Case 写路径。
明确 Pydantic schema：
step_no
action
data optional
要求：
- step_no 正整数；
- 顺序稳定；
- action 非空。
Migration / legacy adapter 可以兼容旧：
list[str]
但新写路径必须结构化。
15. Expected Results
支持：
step_no optional
expected
这样既可以：
对应某一步
也可以：
整体预期
16. Preconditions
统一：
list[str]
不要旧的：
一整块 Text
产品展示时再 join。
17. Tags
旧：
case_type
迁移：
tags[]
例如：
正常
异常
边界
权限
状态
不作为主列表核心列。
18. Source / Requirement
旧：
requirement_id
source
新：
source_refs
Revision actor_type
Requirement：
source_ref(type=requirement)
人工 / Agent 来源：
Revision actor
不要再增加：
source=manual/llm
双重状态。
19. Status
FunctionCase.status 不迁入 TestCase Definition。
不要显示：
active
disabled
draft
pass
fail
Case 列表。
未来：
TestTask / TestExecution
负责：
Not Run
Running
Passed
Failed
Blocked
P09 不实现 Test Execution。
20. V1 Module/FunctionCase 表
由于当前实际数据：
test_modules = 0
function_cases = 0
本轮不用做复杂数据迁移 UI。
但不要马上 drop table。
先：
标记 legacy/deprecated
新功能用例页面不再读取/写入：
/modules
/function-cases CRUD
物理删表留 P10 legacy cleanup。
21. Legacy API
以下进入 deprecated：
/modules/*
/function-cases/*
但本轮不要立即删除 Router，避免旧代码 import / tests 爆炸。
新 FunctionCasePage 不得继续调用它们。
22. RequirementPage 生成保存
这里是例外。
目前 RequirementPage 仍然是用户可访问页面：
Requirement
→ Generate Cases
→ Save
如果保存后仍写：
function_cases
用户回功能用例页将完全看不到。
所以 P09.1 必须处理这条当前用户路径。
23. Requirement Generated Cases 新保存目标
将：
save_generated_function_cases()
新的产品路径改为：
ensure Project Functional Artifact
↓
resolve/create Module
↓
batch add test_case operations
↓
一个 Artifact Revision
生成 10 条 Case：
一个 Revision
而不是 10 次 Revision。
24. Requirement Module 选择
如果 Requirement 已绑定 Module/Module source：
迁移到对应 Artifact Module。
如果没有：
优先：
Requirement title / generation context
→ 创建语义 Module
如果无法判断：
默认模块
不要创建 root 下 Case。
25. Legacy Agent save-candidates
本轮不要重构：
/agent/runs/{run_id}/save-candidates
这属于 legacy CaseGenerationWorkflow compatibility。
继续保留旧行为并标注：
deprecated compatibility
P10 legacy cleanup 再决定删除/迁移。
不要因为 P09.1 把整个旧 Agent Workflow 拉进来。
26. V1 FunctionCasePage 重写
现有：
frontend/src/pages/FunctionCasePage.jsx
从旧：
ModuleTree
+
/function-cases table
改为：
Project Functional Artifact
+
Module Tree
+
Case List
页面位置和 App 菜单保持。
用户仍然点击：
功能用例
进入这个页面。
27. 不做独立 Artifact Workspace
停止：
Chat 页面
+
ArtifactPanel
这种产品布局。
从：
V2ChatPanel
移除 P09.1 ArtifactWorkspace 第三/右侧列挂载。
恢复 Chat 为纯 Agent Conversation UI。
28. 可复用 P09.1 已有代码
保留：
mindMapModel.js
artifactWorkspaceModel.js
api/testArtifact.js
useArtifactViewState.js
以后 P09.2 使用。
ArtifactMindMap / NodeInspector 可以保留组件代码，但 P09.1 暂时不要求挂载。
29. 新 ModuleTree
重写 Module Tree，使数据源为：
Artifact tree
不是：
/modules/tree
节点映射：
node_type=module
→ Module
test_case 不出现在左侧 Module Tree。
30. Module Tree 首期行为
P09.1 只要求：
Select
Expand/Collapse
Module CRUD UI 留 P09.2。
顶部：
全部模块
代表：
scope = artifact root
点击 Module：
scope_node_id = module.id
31. Case List
右侧/主区域显示：
编号
模块
名称
前置条件
步骤
预期结果
优先级
不要再显示：
ID
类型
来源
状态
更新时间
作为主要列。
更新时间可以放详情，不是核心列。
32. 表格展示
前置/步骤/预期可能很长。
Table 中只显示摘要，例如：
前置条件：
已注册账号...

步骤：
1. 打开页面
2. 输入...

预期：
登录成功...
点击行：
打开只读 Case Detail Drawer
P09.2 再正式编辑。
33. Case Detail
P09.1 只读即可。
结构化展示：
编号
模块路径
名称
优先级

前置条件
1.
2.

步骤
1.
2.
3.

预期结果
1.
2.

Tags
SourceRefs
禁止继续 <pre> 原样打印 JSON。
34. Module Scope
点击：
登录
Case List 应显示：
登录 Module 本身
+
所有 descendant modules
下的 Cases。
也就是：
include_children=true
语义。
前端可以从已经加载的 Artifact tree 派生，不必调用旧 path LIKE service。
35. Case List 数据来源
P09.1 首版如果 Artifact tree 已包含完整 Case content：
直接从 tree 派生。
不要：
一个节点一个请求
如果现有 tree API 过重/过轻，再补最小 artifact list/read API。
优先避免新 API。
36. Project Switch
现有 FunctionCasePage 的 project Select 保留。
切换 Project：
resolve project
↓
ensure Project Functional Artifact
↓
load tree
↓
Module Tree
↓
Case List
不能继续读：
test_modules/function_cases
37. Project selection
P09.1 不强制重构全局 Project Store。
继续使用：
testmind:selected_project_id
可以接受。
全局 Project Context 以后再统一。
不要因为本阶段重写三个页面。
38. Empty Project
新 Project 没有 Artifact：
ensure functional artifact
自动得到：
Project root
Module Tree：
暂无模块
Case List：
暂无用例
这是正常空态。
39. 不再显示“无模块用例”
删除当前 FunctionCasePage：
无模块用例
入口。
Case Domain 已禁止无模块。
40. Filters
P09.1 保留有价值的筛选：
Keyword
Priority
Requirement（若可由 SourceRef 查询/前端过滤）
暂时移除旧：
status
source
case_type 单选
类型以后通过 Tags filter 回来。
如果为了 P09.1 工作量，Requirement filter 暂时难做，可以 defer P09.2。
不要为了保留旧 UI 强行保留旧 Domain。
41. Search
Keyword 至少匹配：
case number
title
preconditions
steps
expected
200～500 节点首期允许前端本地过滤。
未来大量数据再做 server pagination/search。
42. Pagination
当前 V1 全量拉取。
P09.1 暂时允许：
项目 Artifact <= 数百节点
本地列表。
不要在这一轮同时设计大型分页系统。
P09.3/P10 性能再评估。
43. Revision
即使 P09.1 UI 只读：
RequirementPage 保存生成 Case 时必须走：
Artifact Service
→ Revision
新 FunctionalCasePage 本阶段不直接写 Case。
44. Permission
继续复用：
project read/write permission
Artifact owner/project permission
Viewer：
可看 Module Tree / Case List
Tester/Admin：
后续 P09.2 可编辑。
P09.1 不要重新实现权限系统。
45. Artifact Tools
因为 node_type 改为：
module
同步 P08 Tool schema / Tool descriptions。
Agent 面向模型的语言直接统一：
module
test_case
不要再让模型同时理解：
group/test_point/module
46. Test Design Skill
同步 Skill 术语：
module
test case
测试设计方法：
正向
异常
边界
状态转换
...
仍然是方法，不要重新引入 test_point 强制层。
Agent 可以直接：
Module
└─ Case
47. Quality Tools
Coverage/Duplicate 适配：
module/test_case
不得因为 node_type 收敛破坏 P08 Eval。
48. P08 Eval
更新 fixtures：
group/test_point
→ module
原有：
24 cases
必须继续通过。
指标语义不变。
49. Backend Tests
新增重点测试：
1. module can nest module；
2. case must parent module；
3. case cannot parent root；
4. module can contain case + child module；
5. default module exactly one；
6. project functional artifact exactly one；
7. two concurrent ensure artifact only one；
8. derived case number stable；
9. move case does not change number；
10. Requirement generated save → Artifact Revision；
11. generated 10 cases → one Revision；
12. old V1 tables unchanged/deprecated；
13. project isolation。
50. Frontend Tests
至少：
1. Project → Artifact load；
2. Module Tree from Artifact；
3. nested modules；
4. select Module → descendant cases；
5. select root → all cases；
6. case columns correct；
7. structured Case detail；
8. Project switch no stale tree；
9. empty project；
10. no “无模块用例” UI；
11. removed V2 Chat Artifact third column。
51. Existing Chat regression
必须确认：
V2ChatPanel
移除 Artifact third column 后：
multi-turn
follow-up
tool activity
composer
history
仍正常 build/test。
P09.3 再将 Chat 以 Drawer 方式嵌入用例页面。
52. P09.1 不做
严格禁止：
MindMap 正式挂载
Module CRUD UI
Case CRUD UI
Diff UI
History UI
Undo UI
Restore UI
Agent Drawer integration
selection context
Agent realtime artifact events
revision conflict UI
Approval
P10 Context
53. P09.2 边界
P09.2 再实现：
[List] [MindMap]

Module:
create
rename
move
delete

Case:
create
update
move
delete

Inspector

Revision
Diff
History
Undo
Restore
54. P09.3 边界
P09.3：
现有 Agent Drawer
selected_module context
selected_case context

Agent主动询问模块
Agent CRUD Module/Case
Artifact realtime refresh
ChatTurn change summary
optimistic conflict
full E2E
55. Legacy cleanup 不在本阶段
不要删除：
test_modules table
function_cases table
module_router
function_case_router
legacy AgentRunner
save-candidates
只停止新页面使用。
最终物理 cleanup 留 P10。
56. Documentation
不要新建设计文档。
更新：
01_DEVELOPMENT_PLAN.md
02_DEVELOPMENT_RECORD.md
03_ACCEPTANCE_CHECKLIST.md
明确记录：
P09 product direction changed:
AI embedded into Functional Case Management,
not standalone Artifact Workspace.
但不要大改历史记录。
57. 最终 P09.1 验收
浏览器：
1. 打开「功能用例」

2. 选择 Project A

3. 自动加载/确保该项目 Functional Artifact

4. 左边显示 Module Tree

5. 默认选「全部模块」

6. 右侧显示所有 Cases

7. Module 可以嵌套显示

8. 点击「账号锁定」
   → 只显示该 subtree Cases

9. Case List 列：
   编号
   模块
   名称
   前置条件
   步骤
   预期结果
   优先级

10. 点击 Case
    → 结构化详情

11. 切 Project B
    → Artifact/Tree/Cases 全部切换

12. F5
    → Project/Artifact 正确恢复

13. 打开 AI Chat
    → 不再出现独立 Artifact 右侧栏
58. 最终报告
完成后必须回答：
1. ArtifactNode 最终 node_type；
2. migration 如何转换 group/test_point；
3. Module Tree 最终来源；
4. Project 如何找到唯一 Functional Artifact；
5. 默认 Module 如何保证唯一；
6. Case schema；
7. Case number 最终算法；
8. FunctionCasePage 删除了哪些旧字段；
9. Requirement-generated Cases 如何写 Artifact；
10. 哪些 V1 API 已停止被新页面调用；
11. 哪些 legacy 路径仍保留；
12. P08 Tool/Skill/Eval 如何适配 module；
13. frontend/backend tests；
14. build/lint；
15. P09.2 deferred。
完成 P09.1 后停止。
不要自动进入 P09.2。